package DTO;

public class Enums {
    enum UserRole { Unknown, General, Administrator }
    enum PriceCategory { Unknown, $, $$, $$$, $$$$ }
    enum RestaurantType { Unknown, Italian, Casual, Southern, Asian }
    enum AttractionType { Unknown, Museum, Theater, Park, Landmark }
    enum TransportationMode { Unknown, Foot, Bike, Car, Transit }
    enum PlaceType { Unknown, Misc, Restaurant, Attraction }
}
